import Carousel from './component'
function myCreate(Class){
    console.log(arguments)
    for(let name in attributes)
        object.setAttribute(name, attributes[name]);
    return object; 
    return new Class;
}
var a = <Carousel width='500'>
</Carousel>