module.exports = function (env = {}) {
  const webpack = require('webpack'),
    path = require('path'),
    fs = require('fs')



  return {
    entry: './src/config.js',
    mode: 'production',
    output: {
      library: "spritejsx", // string,
      filename: 'spritejsx.js',
      path: path.resolve(__dirname, "./dist")
    }
  }
}
